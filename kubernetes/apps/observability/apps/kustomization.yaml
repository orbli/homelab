apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Child applications for the observability stack
# These are managed by the parent App of Apps

resources:
  - loki-app.yaml
  - alloy-app.yaml
  - tempo-app.yaml
  - pyroscope-app.yaml
  # Note: namespace-app.yaml moved to /kubernetes/gitops/apps/
  # Note: Prometheus and Grafana deployed via Helm in playbook 08.2