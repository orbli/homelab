apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: iam

# Note: KeycloakRealmImport is managed by Ansible directly
# This avoids issues with one-time imports and secret substitution
# The realm configuration is applied during deployment via Ansible

resources: []
# Realm configurations are handled by Ansible, not GitOps
# This prevents ArgoCD sync issues with one-time imports