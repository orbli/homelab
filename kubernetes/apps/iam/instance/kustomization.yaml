apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: iam

resources:
# Realm configurations
- orbb-li-realm.yaml
# Note: Master realm is Keycloak's internal admin realm
# User access is managed via keycloak-initial-admin secret