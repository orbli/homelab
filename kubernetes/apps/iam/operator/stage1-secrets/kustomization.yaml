apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: iam

resources:
# Only namespace and secrets - no pods yet
- namespace.yaml
- postgres-secret.yaml
- keycloak-db-secret.yaml