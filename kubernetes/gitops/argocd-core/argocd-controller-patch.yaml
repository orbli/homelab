# Patch to make ArgoCD controller use the self-manager service account
# This enables full self-management capabilities
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: argocd-application-controller
  namespace: gitops
spec:
  template:
    spec:
      serviceAccountName: argocd-self-manager