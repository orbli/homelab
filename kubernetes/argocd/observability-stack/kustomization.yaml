apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Child ArgoCD applications for the observability stack
resources:
  - loki-app.yaml
  - alloy-app.yaml
  # Note: Tempo and Pyroscope are deployed separately via ansible playbooks
  # Note: Prometheus and Grafana are deployed via Helm in playbook 08.2