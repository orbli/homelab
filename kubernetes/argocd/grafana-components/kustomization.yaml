apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Child ArgoCD applications for Grafana components
resources:
  - tempo/tempo-app.yaml
  - pyroscope/pyroscope-app.yaml
  - loki/loki-app.yaml
  # Note: observability-namespace is deployed separately to avoid circular dependencies