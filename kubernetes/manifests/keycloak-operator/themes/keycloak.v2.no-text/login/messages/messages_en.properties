# Message overrides for keycloak.v2.no-text. Only keys listed here are
# replaced; everything else falls through to the parent chain
# (keycloak.v2 -> base) at runtime.

# Drop the "Or" since there's no longer a username/password form above
# the IdP buttons that the "Or" was contrasting with.
identity-provider-login-label=Sign in with
