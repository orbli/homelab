apiVersion: v1
kind: Service
metadata:
  name: remark42
  namespace: default
  labels:
    app.kubernetes.io/name: remark42
    app.kubernetes.io/part-of: comments
spec:
  type: ClusterIP
  selector:
    app: remark42
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
